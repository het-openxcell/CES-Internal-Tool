# Auth Fixtures

Go and Python auth tests use these fixtures as the shared contract for request, success, and error shapes. Field names must remain `token`, `expires_at`, `error`, `code`, and `details`.
